// Código para acessar o valor de um objeto dentro de uma variável usando  pesquisa  indireta, ou seja, enviamos o nome de  uma variável independente que possui o mesmo nome do objeto, o resultado da pesquisa  é  o mesmo, a localização obtém sucesso.
// Faz uso de: Variáveis, Objetos, Acesso de variável por variável e Console.log

// Setup
var testObj = {
  12: "Namath",
  16: "Montana",
  19: "Unitas"
};

// Only change code below this line

var playerNumber = 16;       // Change this line
var player = testObj[playerNumber];   // Change this line

console.log(player);

//A varivável "playerNumber" recebe o valor "16", sem nenhuma ligação direta com o objeto de nome "16" dentro da variável "testObj".
//A variável "player" acessa o valor "Montana" do objeto "16 = "playerNumber" dentro da variável "testObj".
//Por fim, console.log lê a variável "player" que leu o objeto antes.
